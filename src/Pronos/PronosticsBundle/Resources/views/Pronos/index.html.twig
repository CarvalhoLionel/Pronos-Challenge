{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pronos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Scorehometeam</th>
                <th>Scoreawayteam</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pronos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.scoreHomeTeam }}</td>
                <td>{{ entity.scoreAwayTeam }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pronos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pronos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pronos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
