{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pronos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Scorehometeam</th>
                <td>{{ entity.scoreHomeTeam }}</td>
            </tr>
            <tr>
                <th>Scoreawayteam</th>
                <td>{{ entity.scoreAwayTeam }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pronos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pronos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
