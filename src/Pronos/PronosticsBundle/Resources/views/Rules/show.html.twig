{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rules</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pointscorrectresult</th>
                <td>{{ entity.pointsCorrectResult }}</td>
            </tr>
            <tr>
                <th>Pointsexactresult</th>
                <td>{{ entity.pointsExactResult }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('rules') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('rules_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
