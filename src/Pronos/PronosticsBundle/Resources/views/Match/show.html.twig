{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Match</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Scorehometeam</th>
                <td>{{ entity.scoreHomeTeam }}</td>
            </tr>
            <tr>
                <th>Scoreawayteam</th>
                <td>{{ entity.scoreAwayTeam }}</td>
            </tr>
            <tr>
                <th>Datematch</th>
                <td>{{ entity.dateMatch|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('match') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('match_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
