{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Match list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Scorehometeam</th>
                <th>Scoreawayteam</th>
                <th>Datematch</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('match_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.scoreHomeTeam }}</td>
                <td>{{ entity.scoreAwayTeam }}</td>
                <td>{% if entity.dateMatch %}{{ entity.dateMatch|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('match_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('match_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('match_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
